export const propertiesTopic = (deviceId: string) => `devices/${deviceId}/properties`
export const eventsTopic = (deviceId: string) => `devices/${deviceId}/events`
export const commandsTopic = (deviceId: string) => `devices/${deviceId}/commands`
export const configTopic = (deviceId: string) => `devices/${deviceId}/config`
