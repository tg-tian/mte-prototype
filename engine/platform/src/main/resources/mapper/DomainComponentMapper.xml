<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.lowcode.platform.mapper.DomainComponentMapper">
    <!--获取设备类型和领域绑定关系-->
    <select id="selectByDomainAndDeviceType" resultType="demo.lowcode.platform.entity.DomainComponent">
        SELECT * FROM domain_component
        WHERE component_id = #{deviceTypeId} AND domain_id = #{domainId} AND component_type = 'devicetype'
    </select>

    <!--根据设备类型获取设备类型和领域绑定关系-->
    <select id="selectByDeviceType" resultType="demo.lowcode.platform.entity.DomainComponent">
        SELECT * FROM domain_component
        WHERE component_id = #{deviceTypeId} AND component_type = 'devicetype'
    </select>
</mapper>
