## 0624

1、根据类图修改架构，添加Application模块

2、Action节点不要绑定某个具体的设备而是根据要执行的操作分配给该场景下的合适的设备来执行

## 0625

#### Process流程执行优化

1、根据节点的inputParam获取父节点的输出并作为当前节点的输入参数

2、根据节点的condition判断是否需要执行该节点;若不满足条件，则该节点的子节点都不再执行

3、运行时实时修改RTProcess（运行时流程）及节点状态

## 0626 & 0627

1、添加具体设备时根据uri进行添加

2、具体设备的Service代码动态生成并编译

## 0628

实现模拟咖啡机页面

# TODO
* 实现demo页面（从后往前做）：场景页面（场景列表、场景对应资源列表（注册设备）、场景功能列表（Process、应用菜单、发布应用功能））、功能页面、模拟咖啡机页面
* 自定义设备：根据CoffeeMaker.json生成一套对应的java代码，而不是手动写 （模板引擎，Mustache）
* 读取领域、场景、应用的json
* 将元数据持久化，根据需要分设计时和运行时
* controller对外API定义
* bug:具体设备的实现（AService/BService）代码修改后,在不删除上一次生成文件的情况下,本次不会realize,要在下一次运行时才能正确realize。
