<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.lowcode.platform.mapper.DomainComponentMapper">

    <insert id="batchInsertDomainComponents">
        INSERT INTO domain_component (domain_id, component_id, component_type)
        VALUES
        <foreach collection="componentIds" item="componentId" separator=",">
            (#{domainId}, #{componentId}, 'component')
        </foreach>
    </insert>

    <!--获取组件和领域绑定关系-->
    <select id="selectByDomainAndComponent" resultType="demo.lowcode.platform.entity.DomainComponent">
        SELECT * FROM domain_component
        WHERE component_id = #{componentId} AND domain_id = #{domainId} AND component_type = 'component'
    </select>
</mapper>
